<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.databasemp2">

    <application
        android:name=".Data.Database.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DatabaseMP2">
        <activity
            android:name=".UI.CrudActivity"
            android:exported="false" />
        <activity
            android:name=".UI.ViewRoomData"
            android:exported="false" />
        <activity
            android:name=".UI.AddData"
            android:exported="false" />
        <activity
            android:name=".UI.RoomData"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>